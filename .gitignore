# Logs
install.log
dev.log
*.log
npm-debug.log*

# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
dist-ssr/
*.local

# TypeScript incremental cache
*.tsbuildinfo

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite cache
.vite/
.cache/

# Reference repos (three.js / o3de — cloned for study, not part of VREEN)
references/

# Local models (when users upload them at runtime)
public/uploads/

# Electron / electron-builder outputs
release/
dist-electron/
out/
*.exe
*.dmg
*.AppImage
*.deb
*.rpm

# Build resources (track custom app icons; build/ dir itself stays ignored)
!build/
build/*
!build/icon.ico

# npm cache (when using mirror)
.npmrc.local

# Trae IDE local workspace documents (project planning, scratch notes)
.trae/

hello-world/
CLAWDECODE_WELCOME.md
.claude/settings.json

# .vreen build scratch directory (electron-builder temp output)
.vreen-build/
